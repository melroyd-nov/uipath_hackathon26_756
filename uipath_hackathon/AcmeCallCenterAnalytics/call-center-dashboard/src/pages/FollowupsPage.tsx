import PlaceholderPage from '../components/PlaceholderPage';

export default function FollowupsPage() {
  return (
    <PlaceholderPage
      title="Follow-ups"
      description="Per-call follow-ups; approve/reject/assign via embedded HITL forms."
    />
  );
}
